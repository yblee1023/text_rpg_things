<!-- Generated: 2026-08-11 -->

# lastroad — 마지막 국도

## Purpose

단일 파일 한국어 포스트아포칼립스 로드무비. 죽은 내륙을 가로질러 북항의 마지막 배까지
가는 여정. 차의 자리는 셋, 태우려는 사람은 넷 — **누구를 태우고 갈 것인가**가 게임의 전부다.
순수 스크립트 분기, **LLM 호출 없음·빌드 없음·의존성 없음**. 친구에게 URL만 주면 플레이된다.
1회차 6~8분. 엔딩 7종은 localStorage 도감으로 회차 간 누적된다.

이 게임은 **창귀록의 회고에서 나왔다.** 창귀록에서 밟은 지뢰들을 설계 단계에서 구조적으로
피하는 것이 명시적 목표였고, 그 각각을 `check.mjs`가 회귀 가드로 지킨다(아래 표).

## Key Files

| File | 내용 |
|------|------|
| `lastroad.html` | 게임 전체 — ~690줄. inline `<style>`(L8~72) + DOM 껍데기(L74~81) + inline `<script>`(L82~end). 이게 제품이다. |
| `check.mjs` | 자체 점검. `node check.mjs` — 26개 assertion, 의존성 0. `<script>`를 뽑아 컴파일하고, 손수 깎은 DOM 껍데기에 부팅해 **무작위 500판**으로 크래시·막다른 화면·좌석 초과를 사냥하고, **엔딩 7종을 각각 직접 구동**해 도달성을 증명한다. `ok()`가 자체 카운트하므로 합계가 어긋나지 않는다. |
| `README.md` | 플레이·개발 안내. |

`.omc/`는 OMC 운영 상태지 게임 내용이 아니다.

## For AI Agents

### Working In This Directory

- **코드가 진실이다.** 이 문서와 코드가 어긋나면 문서를 고친다.
- **한 파일을 유지하라.** 번들러·`package.json`·npm 금지. 모듈로 쪼개면 "URL만 주면 된다"는 전제가 깨진다. 그게 이 게임의 전부다.
- **테스트는 브라우저로 연다.** URL 끝에 `#dev`를 붙이면 도감이 전부 열린 채로 시작한다(`DEV`, L84). dev 플레이는 도감을 저장하지 않는다.
- 전부 전역 스코프. **함수명이 API다. grep하라, 추측하지 마라.**
- **모든 변경 뒤 `node check.mjs`.** 의존성·설치 없음.

### 창귀록 지뢰 → 이 게임의 구조적 방어 (핵심)

| 창귀록에서 밟은 것 | 여기서 막은 방식 | 지키는 검사 |
|---|---|---|
| **자원 예산이 knife-edge** (발품 한 개만 새도 진엔딩 불가) | ⚠️ **초판은 이 지뢰를 그대로 밟았다** — 7구간에 연료 6으로 원장이 1 적자였고 공급원이 `siphon` 하나뿐이라, 좌초 50%가 추첨으로 결정됐다. 지금은 `fuel:7`/`food:7`로 원장을 맞추고, 연료가 남은 거리보다 모자라면 `pickEnc`가 주유소 가중을 올린다. **원장을 적자로 만들지 마라.** | `[플레이 도달] 연료를 챙기며 완주하면 대부분 항구에 닿는다` |
| **명세와 코드 불일치 / 죽은 분기** (pansaEdge가 도달 불가한데 테스트는 죽은 가지에서 통과) | ⚠️ **초판이 같은 착시를 겪었다** — `alone`은 상태 주입 테스트를 통과했지만 실제 플레이 도달률이 0.0%였다(`hope>=3`과 무동승을 동시에 요구하는데 거절마다 희망이 깎였다). **주입 도달성은 증명이 아니다.** 이제 정책을 정해 끝까지 눌러 보는 플레이 도달 검사가 따로 있다. | `[플레이 도달]` 3종 |
| **항상-참 게이트** (hasYugo가 늘 참이라 두 텍스트 영영 안 뜸) | 게이트를 자원·party·선택으로만 만들고, 각 경로를 테스트가 실제로 밟는다. 양 분기 값이 같은 no-op 삼항(`?0:-0`)도 정적으로 잡는다 — 문자열 검사는 이걸 통과시킨다. | `수치가 같은 no-op 삼항이 없다` |
| **설계가 자기 심장을 억제** | `pickEnc`가 만석일 때 합류 가중을 **껐던** 탓에 좌석 교환 딜레마를 87%가 못 봤다. 지금은 만석일수록 가중을 **올린다**(13%→37%). 핵심 딜레마는 노출률을 수치로 지켜라. | `[플레이 도달] 만석 좌석 교환이 충분히 자주 열린다` |
| **UI가 서사를 반박** | `#hud`(ID, 특정도 1,0,0)가 UA `[hidden]`(0,1,0)을 이겨 "나는 부두에 남았다" 위에 탑승 칩이 떠 있었다. `#hud[hidden]{display:none}` 명시 필요. | `숨김이 필요한 HUD가 CSS 특정도에 지지 않는다` |
| **엔딩에 반영 없는 죽은 상태** (gukVisited·wary) | `INIT()`의 모든 필드가 HUD/씬/엔딩 중 하나에 반영. 특히 `S.flags`에 넣은 값은 **반드시 `has()`로 읽힌다**. | `설정한 플래그는 모두 읽힌다`, `모든 동승자 특성이 쓰인다` |
| **회차 반복 피로** (서장~1막이 매번 동일 → 인트로 8번) | 중간 경유지 인카운터를 **풀에서 무작위 추첨**(`pickEnc`). 인트로는 4노드. 여정이 매 회차 다르다. | — (설계 단계 차단) |
| **단일 실패점이 조용히 판을 브릭** (단서 하나 잃으면 2막 자체 불가) | 자원 고갈은 **막다른 화면이 아니라 엔딩**이다. 연료<0 → 좌초 엔딩, 희망0 → 붕괴 엔딩. 인카운터 풀은 절대 비지 않는다(`repeat` + `FALLBACK`). | `무작위 500판에서 막다른 화면 없음`, `조용한 브릭 금지`, `빈손 추첨 금지` |
| **고증 오류** (활인서 무상기관, 가마/말 신분규정) | 배경이 허구의 포스트아포칼립스라 실세계 고증 함정 자체가 없다. 대신 내부 일관성(좌석 산술)을 검사로 못박는다. | `좌석은 3을 넘지 않는다` |
| **세이브 스키마 버그** ("stale save가 판을 조용히 오염") | **중간 세이브를 아예 두지 않는다.** 1회차가 짧다. localStorage에는 엔딩 도감(문자열 배열)만, try/catch 안에서. | — (기능 부재로 차단) |

### Common Patterns

- **모든 화면은 `show(nodes, choices)`** (L~115). `nodes`는 `[[cssClass|null, html], …]`,
  `choices`는 `[{k:'라벨', t:'설명', f:핸들러, cls?, off?}]`. 씬 함수는 `show()`를 부르고 끝난다.
  핸들러가 다음 씬을 몬다. 라우터·상태머신 없다.
- `paint()` (L~95)가 매 `show()`마다 HUD를 `S`에서 다시 그린다. HUD DOM을 직접 만지지 마라.
- **여정 루프는 `leg → 인카운터 → onward() → drive() → leg`** 로 수렴한다.
  `drive()`가 연료·식량·거리를 정산하고 실패/도착을 판정한다. **모든 씬은 반드시 버튼을 준다** — 빈 화면은 크래시로 취급된다.
- 노드 클래스: `hd`(머리), `sp`(강조·닫는 줄), `said`(대사), null(평문), `note`(회색 메타).
- 데이터는 최상위 테이블에: `COMP`(동승자), `PLACES`, `ENC`(인카운터 풀), `EPI`(배치별 작별), `MARK`(행적 표식), `PARTING`/`LASTWORD`(인물별 작별 대사), `ENDINGS`. **씬이 아니라 테이블을 고쳐라.**
- **배치와 행적은 다른 필드다.** `S.fate[id]`는 배치(`left`/`shore`/`died`, 없으면 탑승 중=`coast`), `S.mark[id]`는 행적(`deal`/`saved`/`blood`). `partyLines()`가 배치 한 줄 + 행적 한 줄을 **덧붙여** 낸다. 한때 둘이 `S.fate` 하나를 다투고 `EPI`에 `deal:''`가 있어서, 발전기를 고쳐 주거나 사람을 살린 동승자가 **엔딩에서 통째로 사라졌다.** 표식을 늘릴 땐 `MARK`에 실제 문구를 함께 넣어라 — 빈 문자열은 인물을 지운다.
- **인물 공용 대사를 쓰지 마라.** 좌석 교환·사망·좌초의 작별은 `PARTING`/`LASTWORD`로 인물마다 갈려 있다. 한 벌로 뭉치면 열 살 아이가 하오체를 쓴다(창귀록 개선계획 항목 4가 이미 제거한 지뢰).
- `repeat:true` 인카운터는 `S.used.has(id)`로 재방문을 판정해 다른 텍스트를 낸다. 그래서 `leg()`는 **`enc.f()` 뒤에** `S.used.add()`를 한다 — 순서를 되돌리면 1회차 텍스트가 영영 안 뜬다.
- 인카운터를 추가하려면 `ENC` 배열에 `{id, f, when?, repeat?, join?, late?, w?}` 객체 하나를 넣는다.
  씬 함수 `f`는 반드시 `onward`(또는 `end`)로 수렴해야 한다.

### State

`S`는 `INIT()` 한 곳에서만 만들어진다. **필드를 늘리면 `INIT()`만 고친다** — 단, Set이면 `SETS`에도 넣어라.
`S`는 `let`이고 `boot()`/`crash()`가 재할당하므로, 씬을 넘겨 사는 지역변수에 캐시하지 마라.

```js
S = { dist, fuel, food, hope,        // 자원 (연료·식량 원장은 dist와 균형을 맞출 것)
      party[], met:Set,              // 동승자·조우 이력
      fate{}, mark{},                // 배치(left/shore/died) · 행적(deal/saved/blood)
      flags:Set, leg, hungry, used:Set, begun, over };
```

**좌석 불변식**: `S.party.length <= SEATS`(=3). `offerSeat()`가 만석일 때 교환 딜레마를 강제해 이 불변식을 지킨다. 새 합류 경로를 만들면 반드시 `offerSeat()`를 거쳐라 — 직접 `party.push` 금지.

### Save Keys

`lastroad.endings.v1` — 엔딩 도감(문자열 배열)만. 진행 세이브는 없다.
`ENDINGS`에 엔딩을 추가하면 `end()`에 같은 key의 `case`를, `markSeen`을 부르는 경로를 함께 넣어야 도감이 샌다(`엔딩 도감 키와 end() 분기가 정확히 일치한다`가 잡는다).

## Known Limits

- `check.mjs`는 **형식·도달성·무크래시만** 보증한다. 프로즈가 진부한지, 톤이 맞는지, HUD가 예쁜지는 사람이 브라우저로 봐야 한다. (창귀록과 같은 한계.)
- 밸런스는 3만 판 시뮬레이션으로 조정했으나 **사람 완주 플레이테스트는 아직 없다.** 체감 난이도는 실측이 필요하다.
- **밸런스를 건드리면 시뮬레이션으로 확인하라.** 정책(사람을 태우는가·연료를 아끼는가)별로 분포가 크게 갈리므로, 무작위 클릭 통계만 보면 오판한다. `check.mjs`의 `[플레이 도달]` 검사가 최소 방어선이다.
- 인카운터 프로즈는 짧게 유지했다(2~4노드). 서사 깊이는 창귀록보다 얕다 — 의도된 트레이드오프(짧은 회차·높은 재플레이).

## Dependencies

없음. 폰트도 시스템 폴백을 쓴다(웹폰트 미로드). 런타임 라이브러리 0.

<!-- MANUAL: 아래 줄부터 수동 메모는 재생성 시 보존 -->
