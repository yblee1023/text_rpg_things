# 창귀록 개선 계획 — 5항목

> 작성: 2026-07-24 · 상태: **5항목 전부 구현 완료** (`node check.mjs` → 29/29)
> 확정된 선택: 분이 **저항(안 B)** · doryeong **6노드** · `이름을 듣고` 억쇠에만.
> 이 문서는 근거 기록으로 남긴다 — 각 변경이 어느 검토에서 나왔는지 추적용.

---

## 요약

| # | 항목 | 판정 | 비용 | 성격 |
|---|---|---|---|---|
| 1 | 죽은 판 신호 | 구현 권장 | 텍스트 3벌 + 수첩 1줄 | 구조 |
| 2 | 서장 건너뛰기 | 구현 권장 | ~10줄 | 반복 피로 |
| 3 | 판윤댁 셋째 아들 | 구현 권장 | 씬 1개 + 1줄 분기 | 서사 |
| 4 | 창귀 3인 개별 해방 | 구현 권장 | 데이터 테이블 1개 | 서사 |
| 5 | 도깨비 sleepy | **삭제** 권장 | −5줄 | 폴리시 |

새 상태 필드 0개. 세이브 스키마 변경 없음.

---

## 항목 1 — 죽은 판 신호

### 문제
압박 실수 하나가 남은 판 전체를 무효화하는데 게임이 말해주지 않는다. 플레이어는 사흘을 더 수사한다.

| 압박 대상 | 잃는 것 | 파급 |
|---|---|---|
| 서운관 | `ilji` | `CHAIN` 연쇄 4명 사망 → `jangbu` 불가 → **2막 진입 자체가 불가** |
| 종친가 | `saju` | 연쇄 2명 사망 → 같은 결과 |
| 의금부 | `yugo` | 2막은 가능, 추격 게이트만 영구 off → **산군 대면 없음** |

검증됨: `S.act2=true`는 `turn2()` 한 곳뿐, 거기 닿으려면 `jangbu` 필요, 공급처는 `gukmu.evid` 단 하나.

### 계획
`resolve()`의 `r.lose` 처리 직후에 손실별 `sp` 한 줄 + `notebook()`의 "아직 모르는 것"에 조건부 한 줄. **둘 다 한다.**

- 즉시 알림만 → 1회성이라 5일째엔 잊는다
- 수첩만 → 사건 순간의 무게가 빠진다
- 게임이 이미 "막히면 수첩을 열어라"로 수첩을 폴백으로 지정했으므로(서장 마지막 줄) 수첩 쪽은 공짜 확장

**세 갈래로 나눌 것.** 한 문구로 뭉치면 의금부의 "2막은 된다"는 차이가 지워져 오정보가 된다.

### 검사
```js
// 세 손실의 파급이 실제로 갈렸는지 — 작중 어휘로만 검사한다
// (설계 용어 '2막'을 텍스트에 요구하면 소설이 망가진다)
uigeum press.text 는 /산군|나례/ 를 포함할 것
seoun·gyusu press.text 는 /산군/ 를 포함하지 않을 것, 그리고 서로 다른 문자열일 것
```

### 리스크
`'bad'` 배지 뒤 삽입 위치를 틀리면 배지-설명 순서가 어색해진다. 텍스트 3벌 증가는 허용 범위.

---

## 항목 2 — 서장 건너뛰기

### 문제
대살록 해금에 엔딩 8종 필요. 서장~1막은 완전 결정론적. **같은 서장을 8번 읽어야 한다.**

### 계획
표지(`boot()`)에 `seen().size>0` 조건부 버튼 + `skipIntro()` 약 6줄.
`intro()`의 서술 9줄만 건너뛰고 **`introB()`는 그대로 재사용** — `give('jeung')`·`mu(8)`·종사관 대사·"닷새/열다섯 번" 안내가 전부 보존된다.

신안 선택은 죽이지 않는다. 무병 자원을 결정하는 선택이라 기본값으로 고정하면 회차마다 유일하게 남은 진짜 선택권이 사라진다.

**`CANSAVE=false`(file://)에서 버튼이 안 뜨는 것은 비문제.** `markSeen()`이 `CANSAVE`를 요구하므로 그 환경에선 8종 수집 자체가 불가능하다. 일관적이다.

### 문구 (확정안)
```js
function skipIntro(){
  show([['hd','서장 — 열아흐레, 청계천 어귀'],
    [null,'열흘 사이 여섯이 사라졌다. 아무도 이들을 찾지 않는다. 그래서 이 일이 나에게 왔다.'],
    ['sp','<b>역적의 자식에게 맡길 만한 일</b>이라는 뜻이다.'],
    [null,'눈밭의 짚 인형을 갈랐다. 종이 두 장이 나온다. 한 장엔 사라진 자의 사주. 다른 한 장은 —'],
    ['sp','비어 있다. 아니, 비어 있는 것이 아니라 <b>지워져</b> 있다.']
  ],[...INTRO_PICK]);
}
```
설계: 서장을 **요약하지 않고 진입점을 뒤로 옮긴다.** 원본 `intro()`의 마지막 두 노드가 이미 신안 선택의 직접 전제이므로 메타 표현이 필요 없다. 제목도 원본과 동일하게 둔다 — 화자는 회차를 모른다.

**초판에서 고친 것**
- `눈밭에 짚 인형이 누워 있다. 배를 갈랐다.` → `눈밭의 짚 인형을 갈랐다.` 한 문장으로 합쳤다. 초판은 한 `null` 노드에 5문장이었는데 원문 `null` 노드 상한은 **4문장**이다(분포: 1문장 12 · 2문장 89 · 3문장 50 · 4문장 6 · 5문장 0).
- **`역적의 자식에게 맡길 만한 일` 두 노드를 되살렸다.** 초판은 원문 서장의 `sp` 4개 중 3개를 버렸는데, `지워져 있다`가 충격이 되려면 그 앞에 *아무도 이 여섯을 찾지 않는다*가 있어야 한다. 한 줄만 살린다면 이것이 값이 가장 크다 — 주인공의 처지와 주제를 한 문장에 담는다.
- `sp`가 씬을 열지 않도록 첫 회상은 `null`로 뒀다(원문: `hd` 직후 `sp`로 여는 서사 씬 **0/61**).

### 선택지 중복 처리
`intro()`와 `skipIntro()`가 같은 2택을 쓴다. `mu(8)`이 두 곳에 생기면 한쪽만 고칠 때 어긋난다.
```js
const INTRO_PICK=[
  {k:'신안 神眼',t:'눈을 뜬다. 보고 싶지 않은 것까지 본다. (무병 +8)',cls:'warn',f:()=>{mu(8);introB(true);}},
  {k:'외면',t:'덮어둔다. 보지 않으면 앓지도 않는다.',f:()=>introB(false)}
];
```
상수 하나, 구조 변경 없음. **2줄짜리 배열에 상수를 만드는 건 보통 과잉이지만, 여기서는 숫자 `8`이 의미적으로 결합된 채 두 곳에 존재하게 되므로 예외로 둔다.**

### 검사
`skipIntro`가 정의부 안에서 `introB(`를 호출할 것 — 프롤로그 텍스트를 중복 작성하는 회귀를 막는다.

### 리스크
`skipIntro`에서 `wipe()`+`S=INIT()` 누락 시 이전 세이브 잔여 상태가 새 판에 섞인다. 표지의 기존 '시작' 버튼과 라벨을 명확히 구분할 것.

---

## 항목 3 — 판윤댁 셋째 아들

### 문제
`BENE`(대살 수혜자) 3인 중 규수는 죄책감 씬, 판사는 악당 대면이 있으나 **판윤댁 셋째 아들은 이름뿐**이다.
셋 중 유일하게 **모르는** 자다. 자기가 억쇠의 목숨을 딛고 산다는 걸 영원히 모른다. 주제의 가장 조용한 판본인데 안 쓰여 있었다.

### 배치 — 활인서에서 나서는 길 (발품 0)
- 새 심문 대상 → 반려. `grill()` 엔진은 압박/회유/물증 7벌 텍스트를 요구하는데 "아무것도 모르는 자"에게 전부 무의미하다. 발품 1도 정당화 안 된다.
- 밤 씬 → 반려. 해방은 밤이고 이 아이는 낮의 사람이다.
- **기존 씬 뒤에 붙임 → 채택.** `jinryo`를 얻고 나서는 길. 이름을 막 안 직후라 타이밍이 최적.

`jinryo` 공급처는 `hwarin()` press·evid 둘뿐이고 `RECALL`은 주지 않으므로 **판당 정확히 1회** 발화한다(검증됨). 활인서는 `gyusu`의 열쇠라 사실상 필수 경유 — 모든 플레이어가 본다.

```js
// resolve() 말미, 1줄
show(n,[{k:'물러난다',t:'수사를 잇는다.',f:(r.win&&r.win.includes('jinryo'))?doryeong:day}]);
```
함수명은 `doryeong`. 이 파일은 **인물 씬 = 로마자 한국어**(`kkokdu`/`seoun`/`gyusu`…), **구조 함수 = 영어**(`intro`/`day`/`free`…)로 계열이 갈려 있다. `gyusu`와 같은 자리다.

### 진실을 말할 선택지는 만들지 않는다
1. 이미 한 번 했다 — `gyusu()` press가 "알고 계셨군요" → 그날 밤 자살. 반복하면 값이 떨어진다.
2. 말할 수 없는 게 아니라 **말하면 안 된다.** 이 이름은 오늘 밤 굴각에게 줄 물건이다. 침묵이 선택이 아니라 **직무**인 게 더 아프다.
3. 분기를 열면 사후 상태 플래그가 필요해진다.

### 초안 A — 6노드 (권장) · 고증 검토 반영 개정본
```js
/* 판윤댁 셋째 — 억쇠 대신 산 자. 아무것도 모른 채로 잘 산다. */
function doryeong(){
  show([['hd','활인서 — 문밖'],
    [null,'문을 나서는데 말 한 필이 들어왔다. 판윤 대감댁이라고, 견마잡이가 문지기에게 일렀다. 열예닐곱쯤 된 도령이 가볍게 내렸다. 겨울인데 뺨이 붉다.'],
    [null,'집에서 보낸 진휼 약재가 닿았는지 물으러 왔다 한다. 종에게 시키지 않고 제 입으로 물었다. 문지기에게 읍을 하고, 나에게도 같은 읍을 했다.'],
    ['said','“어른 먼저 드시지요.”'],
    [null,'포교의 옷을 입은 자에게 판윤댁 자제가 읍을 할 까닭이 없다. 문지기에게는 더욱 없다.'],
    ['sp','물어볼 것이 없다. 이 아이는 아무것도 모른다. 모르는 것이 이 아이의 죄는 아니다.']
  ],[{k:'지나간다',t:'아무 말도 하지 않는다. 할 말이 없어서가 아니다.',f:day}]);
}
```
> `이 아이는 그렇게 배운 것뿐이다`를 뺐다 — 그 문장이 `sp`의 일을 선점하고 `sp` 첫 두 문장이 그걸 되풀이한다. 원문은 사실 층위(`null`)와 판정 층위(`sp`)를 분리한다(별제의 자기변호 L792 → 판정 L797). 지우면 `sp`가 온전히 판정을 진다.

**초판에서 고친 것 (고증 판정 반영)**
- **약값 → 진휼 약재 심부름.** 활인서는 예조 소속 **무상** 구료 기관(유민·역병자)이라 약값을 받지 않았고 양반가 자제를 진료하지 않았다. 약값 받는 곳은 혜민서다. 원문 794행은 별제가 억쇠 이름 옆에 *수혜자* 이름과 결과를 적어둔 것이지 진료 기록이 아니다 — **원문에는 문제가 없었고 초판이 만든 오류였다.**
  부수 효과: 심부름이 제 병과 무관해져 "지난겨울"이라는 소생 시점 누설이 사라진다. 그리고 pq6의 `“그 댁에서 사례가 후하네”`와 붙는다 — 집은 값을 치렀고 아이는 그걸 제 집의 선행으로 안다.
- **가마 → 말 + 견마잡이.** 교자는 2품 이상 당상관용. 판윤(정2품) 본인은 타도 미혼 자제는 말이다.
- **"먼저 드시지요" → 읍 + "어른 먼저 드시지요".** 양반 자제가 중인 포교에게 쓰기엔 과한 하십시오체였다. **신분이 아니라 나이에 대한 예**로 동기화해야 "그렇게 배운 것뿐이다"가 성립한다.
- **문지기에게 먼저 읍한다.** 이 순서 하나가 "포교를 보고 움찔한 것"으로 읽히던 문제를 해결한다. 최하급에게 먼저 하는 예는 습관 말고는 설명이 안 된다.
- **"가볍게 내렸다"를 앞세웠다.** 말을 타고 오면 뺨이 붉은 게 찬바람 탓으로 읽혀 넉 자가 죽는다. 건강을 동작으로 먼저 세우면 뺨이 그 위에 얹힌다.

### 초안 B — 10노드
A와 같은 수정을 반영하되 4노드를 더 벌려 쓰고, 마지막에 두 노드를 붙인다:
```js
    [null,'그가 안으로 들어갔다. 별제는 저 안에 있다. 명단을 넘긴 손과, 그 명단으로 살아난 아이가 한 방에 마주 앉을 것이다. 한쪽만 아는 채로.'],
    ['sp','이 이름은 오늘 밤 눈 위에 선 것에게 줄 것이다. 이 아이에게 줄 것이 아니다.']
```

| | A (6노드) | B (10노드) |
|---|---|---|
| 얻는 것 | 1막 최대 고비 직후의 호흡 보존, `sp` 1회 | 별제-도령 동석의 아이러니, 밤으로의 명시적 연결 |
| 잃는 것 | 아이러니 한 겹 | 활인서 한 호흡이 1막 최장이 됨, `sp` 2회 |

**A를 권장한다.** 활인서는 이미 진료부 획득 + 판윤댁 이름 확립 + "악인이 아니다. 그래서 더 나쁘다"로 끝나는 최대 고비다. B의 마지막 `sp`는 `dokPool` q3이 이미 규칙으로 박아둔 정보라 새로울 게 없고, 버튼 문구가 그 역할의 절반을 한다.

### 설계 메모
그는 끝까지 **이름이 없다**(`BENE[1]`은 직함뿐). 억쇠에겐 이름이 있는데 그를 딛고 산 자는 직함으로만 불린다 — 주제의 반전. 죄책감 0, 예의 100, 건강 100. 그가 죄책감을 느끼면 규수와 겹쳐서 실패한다.

---

## 항목 4 — 창귀 3인 개별 해방

### 문제
`free()`는 텍스트 한 벌로 셋을 처리한다. 억쇠·분이·최노인이 이름만 바뀐 채 똑같이 “...아. 그렇습니까.”라고 한다.
정작 `trueEnd3()`에서는 셋이 각자 다른 작별을 하는데, **놓여나는 순간에는 그 개성이 없다.**

### 계획
데이터 테이블 하나. 공통 뼈대(hd / 그림자·서열 / `got` 배지)는 `free()`에 남기고 몸통만 뺀다.
원본 꼬리 `['sp','수사가 사람을 살렸다.']`는 **삭제** — 세 텍스트가 각자 `sp`를 갖게 되어 중복이고, 세 번 반복하면 값이 떨어진다.

```js
const FREED={
 1:[[null,'억쇠는 이름을 듣고도 한참을 서 있었다. 손이 무언가를 쥐는 모양으로 굽은 채였다.'],
    ['said','“판윤댁이면… 아오. 그 댁에 물을 져다 드렸소. 대문이 높아 뒷문으로 돌았지.”'],
    ['said','“그 댁 도령이 살았소? …그럼 됐소. 물값은 늘 후하게 쳐주던 댁이오.”'],
    ['sp','그는 셈을 틀리지 않았다. 도성이 사람을 그렇게 셈한다.']],
 2:[[null,'분이는 이름을 듣고 고개를 갸웃했다. 열넷이나 되었을까.'],
    ['said','“…그것이 어찌 이름입니까.”'],
    [null,'나는 대답하지 못했다. 그 집 딸의 이름을 나도 모른다. 아무도 적어두지 않았다.'],
    ['said','“저는 분이입니다. 아버지가 그리 불렀습니다. 나리도 그리 불러 주십시오.”'],
    [null,'그 애를 이름으로 불렀다. 두 번 불렀다. 두 번째엔 대답이 없었다.'],
    ['sp','천한 집 아이에게는 이름이 있고, 귀한 집 딸에게는 성씨만 있다. 무엇을 적을지는 관이 골랐다.']],
 3:[[null,'최노인은 이름을 듣고 오래 서 있었다. 입만 달싹였다.'],
    ['said','“…서운관이면, 별 보는 데 아니오? 내가 별하고 무슨 척을 졌다고.”'],
    [null,'농이 아니었다. 정말로 몰라서 묻는 얼굴이었다.'],
    ['said','“…그 양반은 내 이름도 모르겠구려.”'],
    [null,'그가 고개를 숙였다. 관원에게 하는 절이었다. 평생 몸에 밴 것이 마지막에 나왔다.'],
    ['sp','그 양반은 이 사람의 이름을 골랐다. 고르고 나서 잊었다. 나는 그것을 말하지 않았다.']]
};
function free(k,then){
  S.freed.add(k);
  show([['hd','해방'],...FREED[k].slice(0,-1),
    [null,'눈 위의 그림자가 옅어진다. 산군의 서열 하나가 비었다. 오늘 밤 도성에서 아무도 불려나가지 않는다.'],
    ['got',`창귀 ${GHOST[k][0]} 해방 — ${S.freed.size}/3`],
    FREED[k].at(-1)   // sp는 최후미 — 원문 free()와 같은 자리
  ],[{k:'계속',t:'돌아선다.',f:then||dawn}]);
}
```

**`sp`는 반드시 `got` 뒤에 온다.** 원문 `free()`의 노드 순서는 `[hd][null][said][null][got][sp]`로 **`sp`가 최후미**이고, 서사 씬 61개 중 51개(83.6%)가 `sp`로 닫는다. `FREED[k]`를 통째로 앞에 펼치면 씬이 획득 배지로 끝나 이 규약을 깬다. 위처럼 마지막 원소만 떼어 뒤로 보내면 원문과 노드 대 노드로 일치하고, **셋의 차이가 정확히 마지막 한 줄에 실린다.**

### 초판에서 고친 것
- **`원망을 기다렸는데 나오지 않았다` 삭제.** 두 판정관이 **독립적으로 같은 문장을 지목**했다 — 고증 쪽은 "대사가 이미 다 했다", 계측 쪽은 "감정 명사는 원문 0회, 즉시 반려". 원문은 감정을 이름으로 부르지 않고 손·눈·붓·침묵으로만 처리한다(`분노`·`공포`·`죄책감`은 각 1회뿐이며 전부 `vague` 필드 = 흐린 신안 판독 표지).
- **억쇠 `sp` 교체.** `뒷문으로 물을 져다 주던 자가, 앞문으로 드나드는 자 대신 죽었다` → `그는 셈을 틀리지 않았다. 도성이 사람을 그렇게 셈한다.`
  이유: 억쇠가 방금 "대문이 높아 뒷문으로 돌았지"라 말한 걸 `sp`가 되풀이했다. 원문의 `sp`는 되풀이가 아니라 **한 번 더 꺾는다**(`이 사람은 악인이 아니다. 그래서 더 나쁘다`). 교체본은 억쇠의 농담이 실제로 맞다는 것을 화자가 확인해 주는 꺾기다.
- **`그건 이름이 아니지 않습니까` → `그것이 어찌 이름입니까`.** 논변조가 현대적이었다.
- **`관은 성씨만 적는다` → `무엇을 적을지는 관이 골랐다`.** 전자는 사실이 틀렸다(신분 따라 갈라 적었다). 후자는 사실도 맞고 `hwarin`의 "아무도 찾지 않을 사람으로 **골랐**습니다"와 울린다.
- **`아버지가 지어 준 이름입니다` → `아버지가 그리 불렀습니다`.**
  ⚠️ **한 번 잘못 고쳤다가 되돌린 자리다.** 처음엔 `trueEnd3`의 분이 대사(`아버지한테 못 간다고 전해주시오`)와 **충돌**로 보고 아버지를 통째로 지웠는데, 계측 판정관이 **충돌이 아니라 setup–payoff**임을 지적했다 — 이름을 지어 준 아버지가 곧 못 가는 아버지라서 값이 생긴다. 지우면 `trueEnd3`의 그 대사가 허공에서 나온다.
  현재안은 고증 우려(백정가의 공식 작명)를 피하면서 아버지를 남긴다. 덤으로 동사가 `부르다`로 통일되어 게임의 핵심 동사망에 붙는다 — 억쇠가 이름을 부르고(L752), 육혼이 이름을 부르고(L1026), 주인공이 분이를 부른다.

### 각 텍스트의 칼
- **굴각/억쇠** — 그는 판윤댁에 **물을 팔던 사람**이다. 자기를 죽인 집 뒷문으로 물을 지고 돌았고, 원망하지 않고, 제 목숨을 물값으로 셈한다. 항목 3의 도령이 길을 내주는 동작과 앞문/뒷문으로 맞물린다.
- **이올/분이** — "종친가 규수 이씨"는 **이름이 아니다.** 천민 아이에겐 이름이 있고 양반 딸에겐 성씨만 있다. 조선 기록 관행 그대로이면서 주제 직격. 주어를 '관'으로 둬 `pq8`의 "명부는 이름만 본다"와 충돌하지 않는다.
- **육혼/최노인** — 그는 자기를 죽인 자가 **누군지조차 모른다.** 서운관이 뭐 하는 데인지도 모른다. 마지막에 관원에게 절을 한다 — 평생 몸에 밴 것. 주인공은 "그자가 네 이름을 직접 골랐다"를 삼킨다.

### `trueEnd3()`와의 관계
| | 해방 = 놓여나는 순간 | trueEnd3 = 마지막 배웅 |
|---|---|---|
| 억쇠 | 굽은 손만 (`물지게 지던`은 배웅에 양보) | 자기소개, 인사 |
| 분이 | 아버지를 **이름 준 사람**으로 처음 꺼냄 → 배웅 대사의 복선 | 아버지에게 전할 말 |
| 최노인 | 절 (반사적, 신분이 시킨 것) | 감사 — 사람으로서 |

### 억쇠 `sp` — 충돌했다가 해소됨 (개정본 확정)

두 판정관이 같은 줄을 두고 갈렸다가, 계측 쪽이 **자기 권고를 철회**했다.

- 초판 `뒷문으로 물을 져다 주던 자가, 앞문으로 드나드는 자 대신 죽었다.` — 계측 판정관이 처음엔 "원문 L886 계열의 정확한 `sp`, 건드리지 마라"고 했다.
- 개정본 `그는 셈을 틀리지 않았다. 도성이 사람을 그렇게 셈한다.` — 고증 판정관이 "상투적이고 대사를 되풀이한다"며 교체를 요구했다.

**최종: 개정본이 낫다.** 계측 판정관의 철회 사유 — 바로 앞 대사(`물값은 늘 후하게 쳐주던 댁이오`)에서 직접 파생돼 **후킹 거리가 짧고**, `셈`·`틀리`가 원문 0회 신규어다. 초판의 `앞문/뒷문`은 대사의 `뒷문으로 돌았지`를 한 노드 뒤에서 되받는 구조라 거리가 멀다.

### 분이 — 안 B(저항) 확정 · 구현됨

> 완주 플레이테스트에서 톤 확인 완료. *"…그럼 저는 왜 갔습니까"*가 원망·저주 없이 **답 없는 물음**으로만 저항해 원문의 감정 처리 규약을 지킨다. 순응 버전(안 A)은 아래에 기록으로만 남긴다.

**문제**: 세 창귀가 셋 다 원망하지 않는다. 창귀는 지나가는 아이를 불러대는 포식자인데(384행 `“또 봤소. 이번엔 분이였소. 나를 부르지 않고, 지나가는 아이를 불렀소.”`) **3연속 용서는 단조롭고 설정과도 어긋난다.**

저항할 자리는 **분이뿐**이다.
- 억쇠는 안 된다 — `“물값은 늘 후하게 쳐주던 댁이오”`가 세 씬 통틀어 가장 잔인한 줄이고, 잔인한 이유가 원망하지 않아서다. 저항시키면 그냥 화난 유령이 된다.
- 최노인도 안 된다 — 저항할 대상이 없다. 자기를 죽인 자가 누군지 모르는 그 무지가 육혼의 무게 전부다. 게다가 그 수혜자는 판사라 여기서 분노를 쓰면 종막 대면과 겹친다.
- 분이는 384행에 **이미 설치된 총**이다. 그녀가 저항하면 `gyusu()` press의 자살과 짝이 된다 — 한쪽은 죄책감으로 죽고 한쪽은 그 죄책감을 모른 채 묻는다. 획득 순서도 굴각→이올→육혼이라 **순응 → 반발 → 공백**으로 감정이 겹치지 않는다.

**안 B (8노드, 구현본)** — 저항의 형태는 저주가 아니라 **답 없는 물음**이다. (도입 `이름을 듣고`는 억쇠 씬과 겹쳐 제거 → `분이가 고개를 갸웃했다`):
```js
 2:[[null,'분이가 고개를 갸웃했다. 열넷이나 되었을까.'],
    ['said','“…그것이 어찌 이름입니까. 그 아씨는 살아 계십니까.”'],
    [null,'살아 있다고 했다.'],
    ['said','“…그럼 저는 왜 갔습니까.”'],
    [null,'나는 대답하지 못했다. 그 집 딸의 이름조차 나는 모른다. 아무도 적어두지 않았다.'],
    ['said','“저는 분이입니다. 아버지가 그리 불렀습니다. …나리도 그리 불러 주십시오.”'],
    [null,'그 애를 이름으로 불렀다. 두 번 불렀다. 두 번째엔 대답이 없었다.'],
    ['sp','천한 집 아이에게는 이름이 있고, 귀한 집 딸에게는 성씨만 있다. 무엇을 적을지는 관이 골랐다.']],
```
저항의 값은 2노드다. 화자가 답하지 못한 채 넘어가는 것이 저항이고, 그러고도 마지막에 "불러 주십시오"로 돌아오므로 용서가 아니라 **체념 직전의 유예**로 읽힌다.

### 검사
`FREED`의 1/2/3 키가 모두 존재하고 세 배열의 직렬화 결과가 **서로 다를 것**. `resolve()`에 `jinryo`→`doryeong` 분기가 존재할 것.

### 리스크
- `free()`가 `finale()`에서도 호출된다. "오늘 밤 도성에서 아무도 불려나가지 않는다"가 그믐 밤에도 나오는데 **원본 그대로**라 새 리스크는 아니다.
- 도깨비 q9/q10으로 `jinryo` 없이 굴각·이올 이름을 얻는 경로가 있어 항목 3 씬을 안 보고 해방에 닿는 플레이어가 존재한다. 억쇠 텍스트를 항목 3에 의존하지 않게 썼으므로 단독으로 성립한다.

---

## 항목 5 — 도깨비 sleepy 삭제

### 문제
`genial`과 `sleepy` 둘 다 `price:null`, `twist` 없음 — 문구만 다르고 효과가 같다.
sleepy의 "딱 하나만"은 `dokkaebi()`가 질문 하나를 고르면 바로 `day()`로 복귀하는 구조라 **모든 기분에서 이미 참**이다. 거짓은 아니나 정보값 0.

### 계획
`DOK_MOOD`의 sleepy 엔트리 삭제 + `dokMood()`의 두 pool 배열에서 `'sleepy'` 제거 → 3종. **순감 5줄, 순증 0.**

"질문 절반만 노출" 같은 실제 제약을 새로 넣는 안은 +3~4줄인데 서사적 이득이 없다. 삭제가 더 게으르고 더 정직하다.

### 검사
`DOK_MOOD` id 목록이 정확히 `['genial','greedy','trickster']`일 것.

### 리스크
없음. `dokMood()`의 인덱싱이 배열 길이 변화로 재계산되지만 이 값은 저장되지 않는 순수 표시값이다. `check.mjs`에 sleepy 참조 0건.

---

## 톤 검증 결과

원문 408노드를 계측해 기준선을 만들고 초안을 같은 자로 쟀다.

| | 원문 | doryeong6 | doryeong10 | FREED | skipIntro |
|---|---|---|---|---|---|
| `sp` 비율 | 25.0% | 20% | 22% | 18% | — |
| 평균 어절 | 4.7 | 5.0 | 5.1 | 4.3 | 4.1 |
| 중앙값 | 4 | 5 | 4 | 4 | 4 |
| 최대 어절 | 16 | 9 | 12 | 10 | 7 |

- **번역투·현대어 0건.** `에 대해`/`에 대한`/`적인 `/`것 같다`/`라고 생각`/`매우`/`너무나`/느낌표 — 원문도 초안도 0(원문의 `수 있었다` 1회, `!` 2회는 기존).
- **반복 이미지 절제됨.** 원문 `눈 위` 5회·`발자국` 5회인데 초안 전체에서 `눈 위` 1회, `발자국` 0회. 새 씬이 기존 이미지에 기대지 않았다.
- `sp` 비율이 원문보다 **낮다** — 결론을 대신 내려주는 빈도가 원문보다 적다는 뜻이라 안전한 방향.
- 유일한 10어절 초과 문장(12어절)은 초안 B에만 있고, A에서는 잘려 나갔다.

### 개정본 재판정 (15항목 루브릭)

독립 판정관이 원문 L302~L1496을 계측해 15항목 루브릭을 만들었고(즉시반려 5항목 + 감점 10항목), 개정본을 걸어 **전부 통과**했다.

| | doryeong6 | 억쇠 | 분이A | 분이B | 최노인 |
|---|---|---|---|---|---|
| 노드 | 5 | 4 | 6 | 8 | 6 |
| `sp` 비중 (한도 33%) | 20% | 25% | 17% | 12% | 17% |
| 평균 어절 (원문 4.56) | 5.3 | 4.2 | 4.0 | 3.9 | 4.2 |
| 최장 문장 (원문 16) | 10 | 6 | 10 | 10 | 6 |
| 최대 노드 어절 (한도 27) | 21 | 12 | 14 | 14 | 13 |
| C8 감정어 | 0 | 0 | 0 | 0 | 0 |
| C9 금지패턴 | 0 | 0 | 0 | 0 | 0 |
| C14 골격 재사용 | 0 | 0 | 0 | 0 | 0 |
| C5 `sp` 연속 | 없음 | 없음 | 없음 | 없음 | 없음 |
| C6 `sp`로 닫음 | O | O | O | O | O |

### 쟁점 — 창귀 어미 개별화는 규약 위반인가 (해소됨: **아니다**)

리드가 제기한 가설: *원문에서 결박 중 창귀는 `~습니다`로 통일되고 작별에서만 개별화되므로, 해방 순간의 개별화는 규약 위반이다.*

**기각됐다.** 이 가설은 원문에서 **반증 불가능**하다 — 결박 상태 발화는 L1005·L1006·L1016·L1105 넷뿐이고, 앞의 셋은 `left` 배열(1~3명) 전체에 쓰이는 단일 문자열, L1105는 `free(k)`가 k와 무관하게 재사용하는 단일 문자열이다. **개별 집필된 결박 상태 대사는 원문에 0개다.**

독립 반증 셋:
1. **정체성 소실의 표지는 따로 있고 창귀 셋에게는 없다.** L1431 `이름도 모르는 것이 나를 올려다보았다. 얼굴이 흐렸다. 내가 이름을 적지 않았기 때문이다.` + `“…나는 누구요.”` — 이 표지는 **이름이 적히지 않은 프리퀄 무명아에게만** 붙는다. 억쇠·분이·최노인은 `GHOST`에 이름이 있고 이름으로 불린다.
2. **결박 중에도 개별성이 보존된다.** L752 `“…나를 부르더군. 이름을 불렀소.”`(억쇠가 결박 중에 남의 이름을 부른다) · L384 `“이번엔 분이였소.”`(결박 중에 분이로 식별된다). L1006이 요구하는 것도 제 이름이 아니라 **수혜자의 이름**이다.
3. **`trueEnd3`는 변화가 아니라 연속을 명시한다.** L1169 `그가 웃었다. 사흘 전 꼭두쇠를 부르던 그 입으로, 이번엔 인사를 했다.` — 서술이 작별의 목소리를 **결박 시기의 그 입**에 직접 잇는다. 표시된 것은 목소리의 변화가 아니라 용도의 변화(부르던 → 인사)다.

**균일함의 진짜 원인**: `~습니다`는 **공용 문자열의 어체 하한**이다. 억쇠(물지게꾼)·최노인(늙은 걸인)은 하오체, 분이는 열넷 소녀로 `~ㅂ니다/시오`다. 셋에 **동시에** 맞는 어체는 하십시오체뿐이다. 세계의 규칙이 아니라 구현의 제약이다.

**초안이 원문을 읽었다는 증거**: 셋을 다 하오체로 밀지 않고 **분이만 `~습니다`를 남겼다** — L1168(억쇠 하오체)·L1170(분이 `~ㅂ니다/시오`)·L1171(최가 하오체)과 화자별로 정확히 일치한다.

> ⚠️ **완화 조건 — `trueEnd3`의 세 대사(L1168·L1170·L1171)를 절대 고치지 마라.** 해방 씬이 개별화를 앞당기는 만큼, 배웅의 페이오프는 그 세 줄이 손대지 않은 채 남아야 성립한다. 초안이 `억쇠올시다`·`아버지한테`·`최가는 할 말이 없소`를 재사용하지 않았으므로 현재는 안전하다. 나중에 "해방에 맞춰 배웅도 다듬자"는 유혹이 오면 그때 무너진다.

### `이름` 예산 — 기준이 개정됐다 (초안 통과)

루브릭 초판은 `이름`을 1씬 2회로 제한했다(원문 80회, 산문 4,005어절의 2.0%). 분이 씬은 5~6회라 초과였다.

판정관이 **원문을 다시 재서 기준을 고쳤다.** 2.0%는 전체 평균이고, `이름`이 주제인 노드에서 원문이 실제로 쓰는 밀도는 L1275 **22.2%** · L1463 19.0% · L1178 18.2% · L1491 18.2% · L1040 17.6%다. 분이 씬은 10.5%로 **그 절반**이다.

→ **개정 C14**: `이름`이 씬의 controlling idea가 아니면 2회 이내, 맞으면 **어절의 20% 이내**. 예외 면제가 아니라 원문 상한 재측정이다. 세 씬 모두 통과.

다만 **세 `FREED` 텍스트가 모두 `이름을 듣고`로 시작하는 것은 별개 문제**다 — 신규 텍스트 안에서의 자기표절이므로 셋 중 둘의 도입을 바꾸는 편이 좋다.

### 구조 결함 1건 — 셋 다 놓쳤다가 계측에서 잡힘

`FREED[k]`를 통째로 펼치면 씬이 `[got]` 배지로 끝난다. 원문 `free()`는 `sp`가 최후미이고 서사 씬 61개 중 51개(83.6%)가 `sp`로 닫는다. **초안 3벌 모두 이 규약을 깨고 있었고**, 나(계측)·고증 판정관·작가 셋 다 못 봤다. 위 항목 4의 `free()` 코드에 `.slice(0,-1)` + `.at(-1)`로 반영했다.

### 한계

계측은 **형식만** 보증한다. 비유가 진부한지, 인물 목소리가 설득력 있는지는 사람이 읽어야 한다. 고증 판정관이 문학적 판단을 함께 냈고 그 지적은 전부 반영됐지만, 최종 판단은 작가의 몫이다.

---

## 착수 순서 권장

1. **5** (삭제, 리스크 0) → `node check.mjs`
2. **1** (텍스트만, 새 상태 0)
3. **4** (데이터 테이블, 구조 단순)
4. **3** (씬 신설 + `resolve()` 1줄 — 초안 A/B 결정 필요)
5. **2** (`skipIntro` + `INTRO_PICK` 상수 — 유일하게 새 함수)

각 단계마다 `node check.mjs` 통과 확인. 항목별 검사를 함께 추가할 것.
