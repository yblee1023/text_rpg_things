# 창귀록 倀鬼錄 — 설계 명세서

> 리뷰용 문서. 모든 조건은 `changgwirok.html`(단일 파일, ~1550줄) 구현에서 추출한 것이며 기억이 아닌 코드 기준이다.
> 리뷰 시 이 문서와 코드가 어긋나면 **코드가 진실**이고, 문서를 고쳐야 한다.

---

## 0. 한 줄 요약

조선 초 한양. 몰락 양반 출신 포교 **한서린**이 여섯 명의 실종을 쫓다가, 자신이 살아 있다는 사실 자체가 그 사건의 시작이었음을 알게 되는 5일짜리 미스터리.

- **형식**: 단일 HTML, 순수 스크립트 분기 (LLM 호출 없음). 친구에게 URL만 주면 플레이 가능
- **분량**: 1회차 40~50분
- **저장**: localStorage. 엔딩 도감은 회차 간 누적
- **테스트 해금**: URL 끝에 `#dev` 또는 `?dev` → 프리퀄·도감 즉시 열림 (일반 접속엔 미노출)

---

## 1. 세계관과 규칙

### 1.1 핵심 개념

| 용어 | 정의 | 게임 내 역할 |
|---|---|---|
| **제웅(芻靈)** | 짚으로 엮은 인형. 액막이용 | 실종 현장마다 남는다. 배 속에 사주 두 장 |
| **대살(代殺)** | 저승 명부의 이름을 바꿔치기하는 술법 | 사건의 정체. **하나가 살면 하나가 죽는다** |
| **창귀(倀鬼)** | 호랑이에게 먹힌 넋 | 다음 사람을 불러와야 풀려난다 → **자기증식** |
| **산군(山君)** | 산의 주인인 백호 | 악당이 아니라 **채권자**. 제사가 끊겨 굶었다 |
| **도깨비** | 수표교 아래 | 거짓을 못 한다. 물은 것만 답한다 |
| **무병(巫病)** | 신내림 거부로 인한 병 | **게이지이자 자원**. 진실을 볼수록 몸이 상한다 |

### 1.2 절대 규칙 (invariant — 깨면 안 됨)

1. **창귀 해방 조건**: 자기 대신 살아남은 자의 **이름**을 들어야 풀려난다. 수사가 곧 구원.
2. **서운관 판사는 용의자 명부에 없다.** 그는 십이 년 전 제 이름을 스스로 지웠다.
3. **산군은 벌하러 온 것이 아니다.** 오직 끊긴 제사의 복원만이 그를 물린다.
4. **제관은 신을 받은 자여야 한다.** → 도성을 구하려면 사람이기를 그쳐야 한다.
5. **무병 100 = 사망**(창귀화). 무병 60 미만 = 신을 받을 자격 없음.

### 1.3 주제

**"이름을 지우는 나라."** 대살은 목숨이 아니라 이름을 옮기는 술법이고, 이름을 잃은 넋은 놓여나지 못한다. 주인공이 창귀에게 이름을 돌려주는 행위는 아버지가 지운 이름을 되찾는 일이다.

---

## 2. 등장인물

### 2.1 주인공

**한서린(韓瑞獜)** — 좌포도청 포교. 12년 전 아버지 한중원이 역모로 처형되어 '역적의 자식'. 열두 살에 신병을 앓았으나 신내림 거부 → **귀신이 보이되 볼 때마다 몸이 상한다**.

### 2.2 심문 대상 6인

접근 층위(tier)와 심문 성공 조건이 **분리**되어 있다. 볼 수는 있으나 준비 없이 찾아가면 헛걸음.

| id | 인물 | tier | 노출 조건 | 심문 성공 조건 | 획득 단서 |
|---|---|---|---|---|---|
| `kkokdu` | 청계천 다리 밑 — 꼭두쇠 | 1 | 처음부터 | 없음 | `chang` 창귀 목격담 |
| `hwarin` | 활인서 — 별제 | 1 | 처음부터 | `ilji` 보유 | `jinryo` 활인서 진료부 |
| `seoun` | 서운관 — 겁 많은 관원 | 1 | 처음부터 | `chang` 보유 | `ilji` 서운관 관측 일지 |
| `uigeum` | 의금부 — 도사 | 2 | 2막 or `jangbu` | `chingching` 보유 | `yugo` 아버지의 유고 |
| `gyusu` | 종친가 — 병약한 규수 | 3 | `jinryo`/`jangbu`/2막 | `jinryo` 보유 | `saju` 사주 한 쌍 |
| `gukmu` | 성수청 — 국무당 | 3 | `saju`/`jinryo`/2막 | `saju` 보유 | `jangbu` 성수청 굿 장부 |

### 2.3 접근법별 결과 (실측)

각 인물마다 통하는 접근이 다르다. **압박은 위험**하다 — 3명에게 단서 영구 손실을 유발한다.

| 인물 | 압박 | 회유 | 물증 제시 |
|---|---|---|---|
| 꼭두쇠 | 실패 | **성공** | 해당없음 |
| 서운관 | **파국(단서상실)** | **성공** | **성공** |
| 활인서 | **성공** | 실패 | **성공** |
| 종친가 | **파국(단서상실)** | **성공** | **성공** |
| 의금부 | **파국(단서상실)** | 실패 | **성공** |
| 국무당 | 실패 | 실패 | **성공** |

물증 제시는 하위 메뉴에서 **정확한 단서**를 골라야 한다. 헛다리 → 발품 손실.

### 2.4 창귀 3인

| 서열 | 창귀 | 생전 | 대신 살아남은 자(BENE) | 이름을 아는 조건(NAMEOF) |
|---|---|---|---|---|
| 1 | 굴각 | 물지게꾼 억쇠 | 판윤댁 셋째 아들 | `jinryo` 보유 |
| 2 | 이올 | 백정의 딸 분이 | 종친가 규수 이씨 | `saju` 보유 |
| 3 | 육혼 | 늙은 걸인 최노인 | 서운관 판사 본인 | `jangbu` 보유 |

`knows(k) = has(NAMEOF[k]) || S.names.has(k)` — 도깨비 질문으로도 이름 확보 가능(우회로).

---

## 3. 단서 사슬

```
jeung  제웅과 사주 한 장   ← 서장
  ↓
chang  창귀 목격담         ← 꼭두쇠(회유)
  ↓
ilji   서운관 관측 일지    ← 서운관
  ↓
jinryo 활인서 진료부       ← 활인서   ※ 판윤댁 셋째 아들 이름이 여기서 확립됨
  ↓
saju   사주 한 쌍          ← 종친가
  ↓
jangbu 성수청 굿 장부      ← 국무당   ※ 획득 = 2막 전환 트리거
  ↓
chingching 참칭의 증거     ← 도깨비 q7 / 활인서 재심문
  ↓
yugo   아버지의 유고       ← 의금부   ※ 진엔딩 필수
```

---

## 4. 시간 구조

- **닷새제**: 섣달 스무엿새 → 그믐(나례의 밤)
- 하루 = **낮 3발품 + 밤 1회**
- `dawn()`: `S.day++`, `S.acts=3`, `S.day>5`면 `finale()`
- `night()`: `S.day>=5`면 `finale()`

### 예산 (진엔딩 기준)

| | 필요 | 보유 | 여유 |
|---|---|---|---|
| 낮 발품 | 10 (심문6 + 도깨비1 + 추격3) | 15 | +3 (방해 손실 최대 2 감안 시 +3) |
| 밤 | 3 (창귀 해방) | 5 | +2 |

산군은 **종막**에서 대면하므로 밤 예산을 쓰지 않는다.

---

## 5. 전체 흐름

```
서장 (제웅 발견 · 신안 선택 · 아버지 복선)
  ↓
1막 [추리] 낮 심문 사슬 + 밤 무병관리/창귀해방
  ↓
─── 전환점: jangbu 획득 = 국무당 검거 ───
  gukmuVow() 유혹 씬 (필수 경유 — "제관은 신 받은 자여야 한다")
  turn2() 일곱 번째 실종 → 목표 전환
  ↓
2막 [선택] act2=true
  · 창귀 해방 (밤, 방치 시 rank↑ → 8·9·10번째 실종)
  · 판사 추격 3보 (+ 방해 공작 유발)
  · 방해 사건 (낮 시작, 대응/방치)
  · 재심문 (활인서·꼭두쇠)
  · 옥중 재회 (국무당 면회)
  ↓
─── 그믐: 나례의 밤 (finale) ───
  창귀 해방 → 고발 → [판사 검거 시] 산군 대면 → 신받음 판정
```

---

## 6. 엔딩 분기 (정확 조건)

### 6.1 판정 코드 요약

```js
// finale() 내 accuse
if (who !== 'pansa')          → end('wrong')     // 반쪽
if (S.done.has('pansa')) {
    if (S.pansaEdge >= 2)     → end('unravel')   // 무고 (방치로 붕괴)
    else                      → reckoning()      // 산군 대면
}
if (has('chingching') || has('yugo')) → end('thin')   // 무고 (추적 미완)
else                                  → end('nofact') // 무고 (증거 없음)

// reckoning()
'벤다'        → end('slain')  // 사냥꾼
'무릎을 꿇는다' → vowScene()

// vowScene()
canReceive = S.mu >= 60
'받는다'(canReceive일 때만 노출) → perfect = (freed>=3 && has('yugo'))
    perfect  → trueEnd() → 아들 子
    else     → 제관 祭官
'끝까지 사람으로' → 인간 人間

// 상시
S.mu >= 100 → burnout() → 창귀 倀鬼
'침묵'      → end('silent') → 관인 官人
```

### 6.2 엔딩 8종

깊이(관문 수)가 좋은 결말일수록 깊다.

| # | 엔딩 | 등급 | 깊이 | 조건 |
|---|---|---|---|---|
| 1 | **아들 子** | 진엔딩 | 5 | 판사 지목 + 추적완료 + pansaEdge<2 + 무릎 + mu≥60 + 받음 + **창귀3해방 + yugo** |
| 2 | 제관 祭官 | 구원 | 5 | 위와 같되 창귀3 또는 yugo 중 하나 이상 결여 |
| 3 | 인간 人間 | 비극 | 4 | 무릎 꿇었으나 신 거부 (또는 mu<60으로 받기 불가) |
| 4 | 관인 官人 | 침묵 | 1 | 나례에서 침묵 |
| 5 | 반쪽 半 | 미완 | 1 | 국무당 지목 |
| 6 | 무고 誣告 | 파국 | 2 | 판사 지목 but ⓐ증거없음 ⓑ추적미완 ⓒ**방치≥2로 붕괴** (3경로, 텍스트 상이) |
| 7 | 사냥꾼 獵 | 오만 | 3 | 산군을 벤다 |
| 8 | 창귀 倀鬼 | 파멸 | — | 무병 100 도달 (언제든) |

### 6.3 나쁜 엔딩에도 2막 성과 반영

- **관인**: `freed` 수에 따라 문구 변화 + `rank>=3`이면 "이름이 불린 자가 서른을 넘었다"
- **반쪽**: `freed` 수에 따라 "그 셋만은 건졌다" / 부분 반영

### 6.4 진엔딩 전용 연출

`trueEnd()` → `trueEnd2()` → `trueEnd3()` → `trueEnd4()` 4단 시퀀스:
받음(신내림) → 산신제 → 창귀 셋의 개별 작별 → 아버지 복권 + 후일담.

**모든 엔딩 공통**: 통계/도감은 `final()`이 아니라 `record()`(버튼 한 번 더)에서 노출 — 마지막 문장이 화면에 남도록.

---

## 7. 상태 변수

```js
const S = {
  day, acts, mu, buja,          // 시간·자원 (일차, 발품, 무병, 부적)
  over, met,                    // 종료 플래그, 창귀 조우 여부
  act2, rank,                   // 2막 진입, 창귀 서열(0~3)
  pansaGone, chase,             // 판사 도주, 추격 진행도(0~3)
  sabo:Set, wary:{},            // 발동한 방해, 인물별 경계심
  gukSeed, gukVisited,          // 유혹 씨앗 수신, 옥중 면회 여부
  recalled:{}, incidents:Set,   // 재심문 이력, 발동 사건
  pansaEdge,                    // 방해 방치 누적 → ≥2면 검거 붕괴
  clues:Set, freed:Set, done:Set, lost:Set, names:Set, asked:Set
};
```

**전역**: `PQ`(프리퀄 모드), `PQNAME`(프리퀄 이름 분기), `DEV`(테스트 해금), `CANSAVE`.

### 상태 → 엔딩 연결 감사 결과

| 상태 | 엔딩 반영 |
|---|---|
| `freed` | ✅ 아들/제관 갈림, 관인·반쪽 텍스트 |
| `done.pansa` | ✅ 검거/무고 갈림 |
| `mu` | ✅ 신받음 자격, 창귀 엔딩 |
| `pansaEdge` | ✅ ≥2면 무고로 붕괴 |
| `rank` | ✅ 관인 엔딩 텍스트 |
| `wary` | ⚠️ 심문 UI 신호 + 무병 소폭 — 엔딩 직접 반영 없음 |
| `recalled` | ⚠️ 재심문 실익(chingching/pansaEdge/rank/buja)으로 간접 반영 |
| `gukVisited` | ⚠️ 힌트 전달만. 실익 없음 (**미해결 항목**) |

---

## 8. 2막 콘텐츠 상세

### 8.1 방해 공작 (INCIDENTS) — 낮 시작 발동

| id | 조건 | 선택 | 결과 |
|---|---|---|---|
| `recant` | 활인서 심문 완료 | 달려간다(발품1) / 내버려둔다 | 증언 회복 / **pansaEdge++** |
| `summon` | chase≥1 | 응하지 않는다 / 해명(발품1) | 시간 벌기 / 무고 올가미 해제 |

### 8.2 추격 유발 방해 (SABO) — 추격 1·2보 시

`jongsa`(발품 손실) / `false`(가짜 밀고, 무병+4) / `seal`(문서 봉인)

### 8.3 재심문 (RECALL) — 2막, 1회씩

| 대상 | 실익 |
|---|---|
| 활인서 | `chingching` 미보유 시 획득 / 보유 시 **pansaEdge−1** |
| 꼭두쇠 | `rank>0`이면 **rank−1** / 아니면 부적+1 |

### 8.4 도깨비 (12문항)

기분 4종(genial/greedy/trickster/sleepy)으로 변덕. 2막엔 굶주림·심술 비중↑.
`greedy`는 대가로 무병+6. 아는 답은 목록에서 사라짐.
**진엔딩 유도 질문**: q12(제사+제관), q13(무병 임계 — mu<60일 때만 노출).

---

## 9. 프리퀄 「대살록 代殺錄」

**해금**: 엔딩 8/8 수집 (또는 `#dev`). 표지에서 진입.
**플레이어**: 십이 년 전의 **한중원**(아버지). `PQ=true`로 HUD 교체.

```
pqStart → pq1(아들의 신병) → pq2(동무가 대살 발견)
  → pq3(쓴다/덮는다 — 수렴) → pq4(★이름을 묻는가/묻지 않는가)
  → pq5(첫 창귀 목격) → pq6(대립·동무의 진의) → pq7(배신·투옥)
  → pq8(유고 작성) → pqEnd
```

**분기**: `PQNAME` — 대살 제물이 된 아이의 이름(`간난이`)을 묻는가.
- 물으면 → 유고에 이름이 남음
- 안 물으면 → "묻지 않은 것이 이 글에서 가장 부끄러운 대목이다", 그 자리는 12년간 공백

**핵심 반전**: 대살로 처음 살아난 자가 **어린 한서린**. 주인공의 무병은 병이 아니라 *건너온 자리의 표식*. → 진엔딩 제목 「아들 子」와 판사의 대사가 재해석된다.

---

## 10. 검증 체계

`/home/claude/rpg/` 내 jsdom 기반 스크립트. 실행: `node <script>.js 2>&1 | grep -vE "Could not parse CSS|Not implemented"`

| 스크립트 | 역할 | 현재 기준 |
|---|---|---|
| `audit.js` | 전 화면 렌더 + 깨진 텍스트 검사 | 깨진 텍스트 0 |
| `play.js` | 랜덤 500판 크래시/소프트락 | crashes 0 |
| `tr.js` | 전 엔딩 도달성 | 8종 도달 |
| `probe2.js` | 인물별 접근법 성패 실측 | 상기 2.3 표 |
| `tpq.js` | 프리퀄 양 루트 + 해금 게이팅 | 통과 |
| `tedge.js` | pansaEdge 임계(0~1 검거 / 2+ 무고) | 통과 |

> 랜덤 플레이는 의도적으로 관인·반쪽·무고·창귀만 도달한다. 좋은 엔딩은 완주 필요.

---

## 11. 리뷰 요청 사항

1. **미해결**: `gukVisited`(옥중 재회)가 엔딩에 실익 없음. 붙일지 / 힌트 전용으로 둘지
2. **미해결**: `wary`(경계심)가 엔딩에 미반영. 현재는 UI 신호 + 무병 소폭
3. **미검증**: 사람이 처음부터 끝까지 완주한 플레이테스트 없음. 밸런스(발품 압박, 무병 관리 난이도)는 실측 필요
4. **잠재 이슈**: 회차 반복 시 서장~1막이 매번 동일 → 반복 플레이 피로
5. **구조 확인**: 엔딩 트리가 "나쁜 결말=얕음 / 좋은 결말=깊음"으로 설계됨. 이 척추는 유지 전제
